{
  "name": "${PLUGIN_MANUFACTURER_TLD}.${PLUGIN_MANUFACTURER_NAME_LOWER_ID}.${PLUGIN_NAME_LOWER_ID}",
  "version": "${PLUGIN_VERSION}",
  "displayName": "${PLUGIN_NAME}",
  "unity": "2017.1",
  "dependencies": { 
      "com.cycling74.rnbotypes": "0.0.1"
  }
}
